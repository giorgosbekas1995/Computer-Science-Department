#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(void){

char a[11] = "HelloWorld";
char *b;
char c[11];
b = urandom | env LC_CTYPE=C tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1;

for (int i=0; i<11; i++){

c[i] = a[i]^b[i];
printf(" %X \t",c[i]);
}

printf("\n");

}
